# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output and dependencies
# Rust's `target/` and Tauri's generated `gen/schemas` are ignored by
# src-tauri/.gitignore, next to what produces them.
node_modules
dist
dist-ssr
*.local

# Tooling caches
coverage
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Machine-local Claude Code permissions, not project configuration
.claude/settings.local.json
